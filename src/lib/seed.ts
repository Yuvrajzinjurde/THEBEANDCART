
// Seeding script has been cleared as per user instruction.
export const seedDatabase = async () => {
  console.log('Seeding script has been cleared and will not run.');
  return { success: true, message: 'Seeding script cleared.' };
};
